# Task 10 implementation report

## Scope and authority

- Worktree: `/Volumes/Extreme Pro/MYPROJECTS/Transcriptor/build/worktrees/task10-process-tap`
- Branch: `codex/task10-process-tap`
- Starting HEAD supplied by the task: `e818083ac72650dc39638d112eba45a8d8ba8460`
- Starting complete Task 10 diff SHA-256 supplied for this repair: `c407264b057334b25373f66a058dfc94e1f9bba60d179f54eec6e8b5aee2b16b`
- Fourth-repair starting complete Task 10 diff SHA-256 supplied by the task: `e21d6823aa08e5a539f5afe831a2e4e3f68ed76e349dfade580a2068a6a74659`
- Sixth-repair starting complete Task 10 diff SHA-256 supplied by the task: `e866fe154909d737caa24704a557f207a6cd530f157e76f92f7f31e5c4542460`
- Seventh-repair starting complete Task 10 diff SHA-256 supplied by the task: `a334ae56e5c4f93b6ce60c8b934950bc2d4edccbf452f79e5fdd5d230cbdc8ba`
- Eighth-repair starting complete Task 10 diff SHA-256 supplied by the task: `40518a9cd18a9bf23b713d6bf86f7fe04bda7ed93749a857d0346137e178660f`
- Ninth-repair starting complete Task 10 diff SHA-256 supplied by the task: `d4825f5b014759c719700322bbc175f4c0b61a2320bb7392169402a46dd01a29`
- Tenth-repair starting complete Task 10 diff SHA-256 supplied by the task: `b3288ec617801512d20e5906ca6ee58a979af49f9fdf3305637f4cb49f73f1c3`
- Eleventh-repair starting complete Task 10 diff SHA-256 supplied by the task: `f773b8b92c7e111e549897888616d22385e71937108fc4f25488c2599adeb384`
- Twelfth-repair starting complete Task 10 diff SHA-256 supplied by the task: `64a1f7d12c19b974cc1b236058f9884343d0443536a455b0bb0c6f4de7271a04`
- Thirteenth-repair starting complete Task 10 diff SHA-256 supplied by the task: `2ced0edb1daae385fb4a78ad5c470fa9e22585b0d310b953a0ac1df79e9035bc`
- Fourteenth-repair starting complete Task 10 diff SHA-256 supplied by the task: `c407264b057334b25373f66a058dfc94e1f9bba60d179f54eec6e8b5aee2b16b`
- Authoritative brief: `docs/builder/task-10-brief.md`
- Brief intent-to-add SHA-256: `6d1066b7bde3977c0c766deaaf6244468b26daa3ade20427d236bc32a8d87cd3`

The implementation and tests were kept within the brief's create/modify allowlist. No Git mutation commands were run; the fourteenth starting diff was verified with the required read-only hash check.

## Test-first checkpoints

The pre-repair characterization retained from the earlier implementation was 129 tests passing. It is not presented as a historical RED result. The following RED observations were recorded during this repair:

1. The first publication-fence implementation used `memset_s` inside the `CA_REALTIME_API` IOProc. The bridge effects gate rejected it with `function with 'nonblocking' attribute must not call non-'nonblocking' function 'memset_s'`. The fence path now uses an optimization-resistant volatile byte loop, with no non-allowlisted call reachable from the IOProc.
2. `swift test --package-path companion/native-macos --filter ProcessTapSystemAudioSourceTests` initially reported 24 tests with 4 failures while the new exact rollback-order assertion over-specified the `tapUID`, `format`, `aggregate`, and `ioProc` acquisition cases. The expected owned-edge sequences were corrected; a subsequent run reported 24 tests with 1 failure while the `tapUID` expected sequence was corrected, followed by 24/24. The second-repair cleanup-retention, delivery-latch, scratch-lifecycle, startup-abort, overflow-order, teardown-owner, and concurrent-recovery extensions passed 28/28; the third-repair suite passed 32/32; the fourth-repair suite passes 37/37.
3. The cleanup retry test first exposed that a one-shot `stop` failure was being cleared after aggregate destruction without retaining a diagnostic. The source now retains that sanitized failure until an explicit second stop, while retaining/retrying IOProc context and avoiding duplicate successful destruction.
4. A concurrent HAL/watchdog recovery test exposed that a second cause could observe `.running` while the first rebuild was still pending. Recovery-task ownership is now claimed under the state lock, so concurrent causes join the one in-flight rebuild without consuming another budget or terminal-failing it.
5. The optimized release review identified that the IOProc publication fence still used a callback-side `atomic_flag` spin and had a generation-check/write-index TOCTOU. The bridge now uses bounded atomic admission plus an in-flight publication count: the callback never waits, while the non-realtime fence closes admission, stores generation zero, and waits only for already-admitted publishers to quiesce. The deterministic admission/fence test and source/compiler guard pass.
6. Deterministic lifecycle and delivery tests exposed two causal-order gaps: a HAL event can arrive between listener installation and `start`, and an overflow marker can be emitted before retained ring audio. Startup events now abort the owned `.starting` operation before HAL start; retained audio is drained before the overflow marker at the dropped frame's sequence/sample cursor. Pop-time slot zeroization is independently inspected, and teardown waiters carry an owner token so a stale waiter cannot clear a replacement task.
7. An async terminal source failure could leave the controller visibly `.capturing`. Accepted `.failed` updates now publish `.error` with the exact source message, including non-permission and ambiguous failures; tests cover both the exact permission copy and ordinary failure text.
8. The third optimized review found six remaining causal gaps. Publication admission now remains held through slot/ring access, stale zeroization, and counter updates, including a deterministic reopen-during-stale-cleanup race. HAL events during startup or replacement abort the owned operation and cannot return a torn-down graph as successfully running. Overflow episodes are retained as ordered boundaries, so a second episode is emitted after the correct retained audio; sleep and wake share one coalesced recovery episode and budget. Watchdog rebuilds fence and snapshot the ring cursor before teardown, and terminal source failure cleanup is explicitly owned and joined before an immediate retry. These paths are covered by deterministic source/controller/bridge tests without sleeps as proof.
9. The fourth optimized review found six additional lifecycle/ownership gaps. Startup malformed callbacks now record an owner-only abort while `AudioDeviceStart` is in flight; the acquisition owner performs the sole unwind after the HAL call returns. A watchdog expiry recorded during blocked start is replayed after activation. Delivery-queue overflow of an incoming gap retains an overflow marker at the earliest evicted frame before the original gap. Graph handles and teardown ownership are read and mutated under one state-lock discipline, with a deterministic blocked-start/duplicate-stop race. The production IOProc ordering guard is mutation-effective against early admission release, and the tap UID property transfers SDK-retained ownership correctly. Core Audio permission status is normalized across acquisition/property OSStatus edges, with exact denied-copy/controller coverage.
10. The fifth review found that a nonzero device-alive property status was still being treated uniformly as recoverable route loss. The listener event now preserves the raw OSStatus, and `kAudioDevicePermissionsError` is handled synchronously as terminal denial through the monitor before any rebuild-budget claim; other read statuses retain the route-recovery path. Fake live-listener injection covers the exact source teardown/status and controller health/error mapping, including the no-rebuild assertion.
11. The sixth review found seven stale-generation, causal-boundary, saturation, and synchronization gaps. Listener permission observations and running/granted publications now require the owning lifecycle operation as well as generation; terminal status cannot be resurrected by a late publication. Stop waiters carry the teardown owner/epoch, and discarded delivery workers return their earliest queued causal boundary for rebuild gaps. The C bridge records bounded FIFO overflow boundaries with producer cursors and fails loudly if that metadata saturates. Delivery saturation preserves an earliest boundary even when every evicted item is a gap. ScreenCaptureKit status, observer identity, and sequence state now share one lock-backed discipline. Deterministic mutation-effective tests cover the retired-listener, terminal-race, queued-discard, interleaved-overflow, all-gap-saturation, and SCK synchronization paths.
12. The seventh review found six fixed-capacity, producer-cursor, lifecycle, terminal-evidence, monitor-read, and causal-test gaps. Delivery queue admission now reserves replacement markers before eviction, keeps physical count at capacity, and retains a bounded earliest loss marker per identity in stable order, including all-gap and cross-generation saturation. The C bridge records absolute overflow write cursors and bounds retained pops to each captured episode, closing the metadata-poll/pop race. Delivery-worker start, fence, enqueue, and cancellation share a lock-owned state machine; startup advertises running only after worker activation. Terminal failure records every queued discard boundary before final failure and never starts a concurrent gap call ahead of a quarantined in-flight sink; its regression fixture pauses the background drain, proves the queued backlog, and waits on the delivery-fence latch before releasing the in-flight sink. Monitor public getters are lock-backed. New production publication-fence hooks make reopen-during-stale-cleanup mutation tests deterministic, and causal tests use explicit semaphores/latches rather than sleeps or timing as proof.
13. The eighth review found six atomic-initialization, uncrossable-boundary, terminal-ownership, worker-admission, activation, and cursor-wrap gaps. Every dynamically allocated ring atomic is now explicitly `atomic_init`-initialized before publication, guarded by a source-visible completeness test. The C consumer rechecks the producer FIFO after its write snapshot and returns a distinct boundary-pending result, so a marker published between the Swift poll and pop cannot be crossed, even when the ring becomes idle. Terminal failure claims one token and captures its worker/cursor before destructive fencing; concurrent and stale plans cannot touch replacement workers. Delivery dequeue and sink admission are one lock-owned edge, with the fence accounting for queued items and the admitted in-flight owner. A worker is started suspended, its activation lease is prepared under the worker lock, and that lease is released only after the lifecycle owner commits `.running`; a fenced or stale lease cannot publish delivery. Absolute cursors and diagnostic counters stop loudly/saturate before wrap, with monitor-visible terminal evidence and near-limit tests. Metadata-index wrap and malformed index order are also fail-closed. All new evidence is deterministic and mutation-effective without sleeps as proof.
14. The ninth review found five ownership, deadline, lifecycle-ordering, generation-reopen, and counter-saturation gaps. Terminal failure now claims ownership before destructive work, then fences and snapshots converter/ring state under the established realtime-ring lock order, so an in-flight conversion cannot be rejected without a causal boundary. Delivery stop checks an expired deadline before dequeue/admission and safely accounts for any already-admitted invocation; a queued-expired restart test proves no sink call begins. Sleep/wake listener callbacks are serialized through a per-generation FIFO owner, preserving an early wake behind a delayed sleep and coalescing one replacement recovery. Nonzero ring-generation reopening retires and zeroizes queued old-generation slots, clears overflow metadata, and opens only an empty new generation. A diagnostic counter is seeded at `UINT64_MAX - 1` and proven to saturate at the maximum rather than wrap; the source mutation guard is negative-tested. The new ninth evidence is deterministic and mutation-effective without sleeps as proof.
15. The tenth review found terminal raw-ring backlog loss, nonzero-reopen publication-token ABA, incomplete direct-reopen retirement evidence, an over-broad realtime AST allowlist, and stale-cleanup latency proportional to configured slot capacity. Terminal failure now captures the precise queued raw-ring cursor before purging the ring and emits that boundary before final failure when no conversion has begun. Publication admission carries a monotonically changing close/reopen epoch, so a paused pre-admission token cannot publish into a later generation; direct nonzero reopen retires and zeroizes queued payload and overflow metadata before opening the replacement. The callback AST allowlist now rejects `memset_s`, while the production stale zeroization loop is bounded by the actual copied byte count (`totalBytes`) rather than the configured slot capacity. Deterministic bridge/source tests cover the ABA fixture, direct purge and metadata retirement, raw backlog evidence, negative callback reachability, and bounded stale cleanup without sleeps as proof.
16. The eleventh optimized replay reproduced `testConcurrentBlockedStartAndDuplicateStopsSerializeGraphOwnership` twice in 15 total repeats, including under `LIBDISPATCH_COOPERATIVE_POOL_STRICT=1`. The failure was in the fixture orchestration: synchronous `DispatchSemaphore.wait` parked cooperative executor threads immediately after inherited task creation, while the fake HAL's block state and counter reads were not synchronized; the production ownership path was not shown to fail. The fixture now uses lock-backed block/counter state and scheduler-independent async signals with bounded timeout guards only, plus source test hooks that suspend acquisition and shared teardown owners without blocking the cooperative executor. The repaired target passed 30/30 normal optimized repeats and 30/30 optimized strict repeats through the direct XCTest runner. The direct-reopen test now seeds nonzero overflow evidence, inspects complete metadata-array byte zeroization through a bool-only test hook, and has an exact negative source guard that fails if only the metadata `memset_s` is removed. One earlier suppressed full-suite repeat loop returned a nonzero status without retaining its failing output; a diagnosed rerun reproduced no failure and completed 50 consecutive optimized full-suite runs at 195/195 each. The eleventh final package runs passed 22/22 bridge, 53/53 source, 195/195 debug, and 195/195 optimized tests; the release app build, property-list lints, and shell syntax check also passed. No raw metadata bytes leave the C bridge, and TSan remains unavailable under platform policy rather than green.
17. The twelfth replay reproduced the remaining release-only lifecycle flake: after two stops joined a blocked acquisition, the newer stop could finish and return the source to idle, then a stale waiter could create a fresh empty teardown task and put the lifecycle back into `.stopping`; a subsequent start silently no-op'ed, so the fully-owned graph subcase saw zero HAL cleanup edges. `teardownGraph` now accepts the stopping operation token for stop callers and refuses a stale waiter when no owned task remains; the startup cancellation path leaves final `.stopped` publication to the winning stop owner. The target then passed 100/100 sequential optimized invocations and 50/50 optimized invocations through the strict cooperative-pool direct XCTest runner. Terminal failure now uses a non-realtime C close-admission/wait/snapshot/retire operation: a production IOProc admitted between terminal claim and purge is included in the raw-ring boundary before the ring is zeroized. The raw-backlog source test injects that callback at the terminal-retirement edge, and a production callback-path bridge test proves the returned retirement cursor is captured before purge. Metadata inspection now closes and quiesces publication before reading producer-owned bytes; the direct-reopen test explicitly reopens only after inspection. The final twelfth focused suite passed 76/76, the full debug suite passed 196/196, the full optimized suite passed 196/196, and an explicit-status optimized full-suite replay passed 30/30 runs at 196/196 each. The release app build, property-list lints, and shell syntax check passed. TSan remains unavailable under platform policy rather than green.
18. The thirteenth repair closes the final production-terminal, shared-stop, stale-ScreenCaptureKit, and duplicate-retirement findings. The real `TarsRealtimeAudioIOProc` now records an admitted-but-discarded post-copy boundary through fixed atomic state before releasing publication admission; terminal retirement returns that `hadAdmittedLoss` outcome and the source emits the conservative ordered raw gap before final failure. The deterministic bridge schedule drives the production callback path at that post-copy edge and verifies one retirement fence, no enqueued payload, the admitted-loss cursor, and stale zeroization; a source guard mutation that removes the accounting path fails. Stop callers now share one state-owned stop task/completion edge before lower teardown is created, so concurrent callers cannot return early or clear a later owner; the fully-owned graph fixture holds both callers before releasing the shared owner and proves exactly one stop/IOProc/aggregate/tap cleanup. ScreenCaptureKit callbacks carry the current stream owner identity through the lock-backed accumulator/relay mutation edge, and the retired-stream fixture proves an old callback cannot mutate a replacement stream while the valid replacement remains zero-based. Terminal-retired ring ownership is transferred to teardown, and the C terminal API claims an atomic one-shot retirement owner, so generic fence/destroy paths and duplicate terminal callers do not perform a second terminal transition; exact hook counts remain one. The final thirteenth package run passed 198/198 in debug and 198/198 optimized; focused optimized runs passed 24/24 bridge, 53/53 Process Tap source, and 11/11 native capture tests. The optimized fully-owned duplicate-stop target passed 100/100 normal SwiftPM invocations and 50/50 direct XCTest invocations with `LIBDISPATCH_COOPERATIVE_POOL_STRICT=1`; the strict SwiftPM wrapper was stopped after it stalled in planning/execution, while the direct runner completed the required strict evidence. The production admitted-loss overlap and retired-stream identity tests each passed 100/100 direct optimized invocations, and the full optimized direct runner passed 30/30 runs at 198/198. The release app build, property-list lints, and shell syntax check passed after the thirteenth edits. TSan remains unavailable under platform policy rather than green.
19. The fourteenth repair replaces the prior self-fencing/synchronous fixture with an externally controlled production-IOProc publication edge. `TarsRealtimeAudioIOProc` can now hand off one bounded post-copy publication admission to a non-realtime test control actor without waiting, spinning, blocking, changing `activeGeneration`, or releasing the admission from the callback; the control actor observes `PublicationPauseReady`, terminal retirement closes generation/admission and waits, then `ResumeHeldPublicationForTesting` performs the final generation check and records `hadAdmittedLoss` before releasing the gate. The old `FenceBeforeNextPublication`/`DeferNextFinalPublication` self-fencing APIs and schedule were removed, and the bridge regression now has a negative source guard against callback-side generation fencing/atomic locks. The source terminal-retirement fixture drives the same real IOProc symbol through that external hold, proves no retained raw slot exists, proves terminal close is waiting, resumes the admitted callback, and requires exactly one ordered `.unknownEnd` gap at sequence/sample zero before the exact permission-denied failure state. Its dynamic no-retained-slot case is mutation-effective against removal of `|| rawRetirement.hadAdmittedLoss`; the bridge and source schedules each passed 100/100 explicit-status optimized repeats. Focused optimized runs passed 24/24 bridge and 54/54 Process Tap source tests; full debug and optimized package runs passed 199/199. The release app build, property-list lints, shell syntax check, and `git diff --check` passed. TSan remains unavailable under platform policy rather than green.

Final GREEN results:

| Command/filter | Result |
| --- | --- |
| `ProcessTapRealtimeBridgeTests` | 24/24 passed |
| `ProcessTapSystemAudioSourceTests` | 54/54 passed |
| `ProcessTapPCMConversionTests` | 11/11 passed |
| `SystemAudioEngineSelectorTests` | 4/4 passed |
| `SystemAudioCaptureMonitorTests` | 9/9 passed |
| `NativeCaptureSourceTests` | 11/11 passed |
| `CompanionSessionControllerTests` | 17/17 passed |
| Concurrent blocked-start/duplicate-stop target, optimized normal repeats | 100/100 passed |
| Concurrent blocked-start/duplicate-stop target, optimized strict repeats via direct XCTest | 50/50 passed |
| Full `swift test --package-path companion/native-macos` | 199/199 passed, 0 failures |
| Full `swift test -c release --package-path companion/native-macos` | 199/199 passed, 0 failures |
| Full optimized suite repeat replay | 50/50 runs, 195/195 each, 0 failures in diagnosed rerun |
| Twelfth full optimized suite repeat replay | 30/30 runs, 196/196 each, 0 failures with explicit status checks |
| Thirteenth concurrent stop target, optimized normal repeats | 100/100 passed with explicit status checks |
| Thirteenth concurrent stop target, optimized strict direct XCTest repeats | 50/50 passed with explicit status checks |
| Thirteenth production admitted-loss overlap target, optimized direct repeats | 100/100 passed with explicit status checks |
| Thirteenth retired-stream identity target, optimized direct repeats | 100/100 passed with explicit status checks |
| Thirteenth full optimized direct suite repeat replay | 30/30 runs, 198/198 each, 0 failures with explicit status checks |
| Fourteenth external production-IOProc hold target, optimized direct repeats | 100/100 passed with explicit status checks |
| Fourteenth source terminal admitted-loss target, optimized direct repeats | 100/100 passed with explicit status checks |
| `swift build -c release --product TarsCompanionApp --package-path companion/native-macos` | succeeded |
| `plutil -lint companion/native-macos/Resources/TarsCompanionApp-Info.plist` | OK |
| `plutil -lint companion/native-macos/Resources/TarsCompanionApp.entitlements` | OK |
| `bash -n scripts/release_menubar_app.sh` | succeeded |
| TSan native suite | unavailable under the platform policy; not green and not used as qualification evidence |

The release app build completed successfully after the fourteenth repair. The latest optimized test replay completed 199/199 with zero failures and no test-source pointer-capture warnings after the lifetime-bounded `@unchecked Sendable` fixture owners were added.

## Implemented contract areas

- Added the `TarsRealtimeAudioBridge` C target with a preallocated fixed SPSC ring, explicitly initialized atomic producer/consumer indices, counters, generation/publication gate, and overflow metadata cursors, fixed descriptor classification/counters, slot metadata/ASBD/timestamps, a callback-never-waits generation publication protocol, an externally controlled test-only post-copy publication handoff that preserves the callback's admission until a non-realtime actor completes the final check, optimization-resistant zeroization bounded to actual copied bytes during stale cleanup, absolute producer-cursor overflow boundaries, uncrossable consumer boundary checks, saturating diagnostics, and deterministic publication/stale-cleanup fence hooks. The compiler-effects and AST source gates include negative allocation/reachability fixtures and reject callback-side locks/spins, callback-side generation fencing, and `memset_s` reachability; a test-only bool inspector proves direct nonzero reopen clears every overflow-metadata byte without exposing raw metadata.
- Added the Process Tap HAL boundary and CoreAudio implementation for the exact global exclusive, mono, private, muted-off tap scope, fresh aggregate UID creation, IOProc ownership, actual `kAudioDevicePropertyDeviceIsAlive` reads, and listener ownership. Listener registrations and HAL IDs are retained until successful cleanup; teardown continues across failures and retries retained edges without double-success. Core Audio permission-denial OSStatus is normalized at every acquisition/property throw edge; the device-alive listener preserves its raw read status so the source can terminalize permission denial while retaining generic route recovery for other statuses. SDK-retained tap UID ownership is transferred with `takeRetainedValue()`.
- Added `ProcessTapSystemAudioSource` acquisition/rebuild/stop sequencing, a serialized tokenized terminal-failure owner, synchronous listener generation/operation fencing including `.starting` and replacement abort paths, owner-only startup unwind around in-flight HAL start, old-generation drain discard with every bounded per-identity queued-worker boundary, continuously observed realtime counters, bounded delivery and quarantine, causal timestamp/ordered multi-episode overflow gaps including incoming-gap, all-gap, and cross-generation queue saturation preservation, generation-fenced health observation, one shared automatic rebuild budget across every HAL/watchdog/sleep-wake cause, coalesced in-flight recovery ownership so concurrent causes join one rebuild, lock-owned worker start/fence/dequeue/admission transitions, watchdog cursor snapshots taken before teardown, and teardown owner tokens that preserve retry ownership across waiter races. Worker activation is committed before the source advertises `.running`, and terminal evidence is captured before destructive fence/cleanup. All graph fields are guarded by the common state-lock discipline without holding it across HAL calls or awaits. The concurrency fixture uses explicit async acquisition/teardown gates and synchronized fake-HAL state, so strict cooperative scheduling exercises the same ownership edges without a parked executor.
- Deferred the 1 MiB drain scratch allocation until an active start, then zeroized and released it after safe stop; idle construction no longer allocates the scratch buffer. Ring slots are also proven zeroized at pop time independently of destruction hooks.
- Added causal off-realtime conversion to exactly 16 kHz mono PCM16/800 samples per frame for the supported planar/interleaved 44.1/48 kHz mono/stereo input shapes, including fractional resampling and timestamp discontinuity detection. Explicit Core Audio flags are preserved, omitted fixture flags are inferred, PCM16 signedness is enforced, and all-NaN/Inf actual-channel content fails loudly.
- Added the engine selector and app-only `--system-audio-engine auto|process-tap|screen-capture-kit` override. Automatic selection uses ScreenCaptureKit through macOS 14.3 and Process Tap from macOS 14.4; explicit Process Tap below the public API boundary fails loudly. CLI source selection remains SCK.
- Added source health observer tokens and controller identity/token/generation fencing, including visible `.denied` health only for the exact Process Tap permission-denied copy and exact preservation of permission and non-permission terminal failure messages in the controller `.error` state. ScreenCaptureKit capture sequence state is reset before start and remains zero-based.

## Files changed

Created:

- `companion/native-macos/Sources/TarsRealtimeAudioBridge/include/TarsRealtimeAudioBridge.h`
- `companion/native-macos/Sources/TarsRealtimeAudioBridge/TarsRealtimeAudioBridge.c`
- `companion/native-macos/Sources/TarsNativeCompanion/SystemAudioEngineSelector.swift`
- `companion/native-macos/Sources/TarsNativeCompanion/CanonicalSystemAudioConverter.swift`
- `companion/native-macos/Sources/TarsNativeCompanion/ProcessTapHAL.swift`
- `companion/native-macos/Sources/TarsNativeCompanion/SystemAudioCaptureMonitor.swift`
- `companion/native-macos/Sources/TarsNativeCompanion/ProcessTapSystemAudioSource.swift`
- `companion/native-macos/Tests/TarsNativeCompanionTests/ProcessTapRealtimeBridgeTests.swift`
- `companion/native-macos/Tests/TarsNativeCompanionTests/SystemAudioEngineSelectorTests.swift`
- `companion/native-macos/Tests/TarsNativeCompanionTests/ProcessTapPCMConversionTests.swift`
- `companion/native-macos/Tests/TarsNativeCompanionTests/SystemAudioCaptureMonitorTests.swift`
- `companion/native-macos/Tests/TarsNativeCompanionTests/ProcessTapSystemAudioSourceTests.swift`
- `docs/builder/task-10-report.md`

Modified:

- `companion/native-macos/Package.swift`
- `companion/native-macos/Sources/TarsNativeCompanion/CaptureSource.swift`
- `companion/native-macos/Sources/TarsNativeCompanion/CompanionSessionController.swift`
- `companion/native-macos/Sources/TarsNativeCompanion/ScreenCaptureKitSystemAudioSource.swift`
- `companion/native-macos/Sources/TarsCompanionApp/TarsCompanionApp.swift`
- `companion/native-macos/Tests/TarsNativeCompanionTests/NativeCaptureSourceTests.swift`
- `companion/native-macos/Tests/TarsNativeCompanionTests/CompanionSessionControllerTests.swift`

## Evidence ceiling and uncertainty

The verification evidence is offline only. The fake-HAL fixtures include exact acquisition rollback and injected cleanup-failure retry coverage.

The evidence is limited to local Swift/C compilation, deterministic fake-HAL fixtures, source/AST/compiler gates, debug and optimized package tests, release app compilation, property-list linting, and shell syntax checking. The TSan suite is unavailable under the platform policy and is explicitly not green. This evidence does not establish live CoreAudio Process Tap acquisition, TCC/permission behavior, actual realtime scheduling/performance, hardware/device behavior, signing/notarization/stapling, installation, provider/backend/frontend behavior, network behavior, deployment, or production readiness. The release script was syntax-checked only and was not run.

No live capture, TCC/permission request, provider or network contact, device use, deployment, production action, signing/notarization/stapling, release-script execution, or Git operation occurred.

The converter is implemented as a causal off-realtime PCM conversion/resampling path with the required observable behavior; these offline tests do not prove implementation-specific behavior of a live `AVAudioConverter` or a live CoreAudio graph.
