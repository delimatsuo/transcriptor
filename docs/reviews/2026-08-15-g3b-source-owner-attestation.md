# G3B Source-Only Owner Attestation

This attestation is for the local source/offline checkpoint only.

## Scope

I reviewed the G3B source implementation against authorization package
`10e6411f65ffba9d1b137d0b7ebba3e6f8f5c388` / tree
`5c747f93592a21e29254e610aa1d0bbeb324828f` and the exact parent
`47fc798885be4d09d983d16ddc14c26a1c90d366` / tree
`160584b1fd9ddcb436a9c158f8658a6c6415fda3`.

The source implementation checkpoint is commit
`ac60992eb7e5259bb866646304eeb6e8abbf0932` / tree
`c1df248baef48cb84dc2447e06fbf7d533456e25`.

The implementation is limited to the approved G3B companion, test, guard,
sandbox, and evidence paths. No existing protocol, backend, frontend,
extension, PR, protected worktree, or unrelated path was changed.

## Safety and claim ceiling

The offline execution path uses generated bytes and injected source contracts.
Native adapter declarations fail closed unless a separate authority exists;
offline tests do not invoke them. The implementation does not request
permissions, access devices, start capture, select virtual/default routes, call
providers or cloud services, open network connections, load credentials, write
raw-audio artifacts, or use participant/candidate data.

The evidence claim is limited to deterministic source/offline behavior. It is
not evidence of physical erasure, provider delivery/deletion, gateway
coverage, device reliability, hosted integration, pilot readiness, release, or
launch readiness.

## Required next gate

Before any publication, controlled fixture, device, permission, provider,
network, cloud, credential, deployment, release, or merge action, rebind the
exact source commit/tree and obtain the separately required review/authority
for that action.
