# G3B Source-Only Offline Evidence

Status: local source checkpoint; not published and not a device, provider,
hosted, deployment, release, or real-data result.

## Exact binding

- authorization package commit: `10e6411f65ffba9d1b137d0b7ebba3e6f8f5c388`;
- authorization package tree: `5c747f93592a21e29254e610aa1d0bbeb324828f`;
- implementation parent commit: `47fc798885be4d09d983d16ddc14c26a1c90d366`;
- implementation parent tree: `160584b1fd9ddcb436a9c158f8658a6c6415fda3`;
- source implementation commit: `6255e1fe093d5b135551058d91ee1ad2f68b157a`;
- source implementation tree: `662c0ca1168d7cca954a98c19873a86ebd8c9abd`;
- implementation worktree: `/private/tmp/transcriptor-native-g3b-source`;
- implementation branch: `codex/native-g3b-source`;
- source paths: the exact runtime, test, guard, sandbox, and evidence paths in
  the approved G3B source-path map.

## Implemented corridor

The source-only package contains generated-fixture companion contracts,
independent microphone/system-audio identities, canonical event and coverage
identifiers, sequence/gap reduction, sample-rate-derived two-second custody,
local zeroization, provider-effect fencing, lifecycle/deletion quiescence,
content-free diagnostics, and fail-closed native adapter declarations.
The remediation checkpoint additionally keeps reducer and trace state
metadata-only, binds event context to device and monotonic/wall-clock fields,
validates frozen atomic and terminal coverage vectors, rejects
generation-mismatched or post-discard effects, requires callback quiescence
before deletion, rolls back failed custody reservations, rejects incomplete
gap bounds, and bounds diagnostic and terminal-history maps.

The AVAudioEngine/Core Audio microphone boundary and ScreenCaptureKit audio-only
boundary are declared for macOS 13.0+, but no permission request, device
access, stream start, virtual/default route, provider, network, credential, or
cloud path is invoked by this checkpoint.

## Verification

Commands run from `companion/native-macos`:

```text
swift test --disable-sandbox                         PASS (29 tests)
Scripts/run_g3b_offline_guard.sh                     PASS twice
Scripts/run_g3b_artifact_scan.sh                     PASS
git diff --check 47fc7988..HEAD                      PASS
```

The offline guard runs Swift tests with networking denied by the local sandbox
profile and rejects native adapter references from tests, live/network APIs,
virtual-route names, raw-audio extensions, and credential patterns. The
artifact scan rejects out-of-scope paths and app, bundle, audio, or credential
artifacts. Test evidence is deterministic generated bytes only.
This source checkpoint has not received fresh independent staff or
security/privacy approval after remediation; those exact-tree reviews are the
next gate.

## Claim ceiling and stop conditions

This record supports only deterministic source/offline behavior at the exact
checkpoint. It does not establish native-device reliability, permission
success, physical erasure, provider forwarding/deletion, durable gateway
coverage, hosted integration, pilot readiness, deployment, release, or launch
readiness. Any controlled fixture, live device, permission, stream, provider,
network, credential, cloud, or real-audio action requires a separate named
authorization.
