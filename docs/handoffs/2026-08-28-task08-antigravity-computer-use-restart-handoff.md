# Task 08 Antigravity Computer Use Restart Handoff

> **Archived historical snapshot — not current continuation state.** Preserved for provenance only. Do not execute its objective or transport procedure, and do not treat its branches, heads, dirty-worktree, PR, application/runtime, crash, or Antigravity state as current. Use the newest durable handoff and exact-head PR/CI/review records.

Created: 2026-08-28 22:54 America/New_York
Prepared by: Codex

## Objective

Restore safe control of the existing Antigravity Transcriptor project so Antigravity can be used again as an authorized builder without opening extra windows or losing the qualified Task 08 state. A full ChatGPT/Codex restart did not repair the signed OpenAI Computer Use service, but Antigravity's existing Electron page is now causally controllable through its own restart-discoverable Chrome DevTools endpoint.

## Resolution Update

- Status: workaround qualified; native Computer Use defect remains.
- Full ChatGPT/Codex quit and relaunch completed. Fresh host and Computer Use client processes did not change the failure.
- A post-restart semantic `set_value` against Antigravity reproduced `Sky Computer Use native pipe closed before response` and created `/Users/delimatsuo/Library/Logs/DiagnosticReports/SkyComputerUseService-2026-08-28-225451.ips`.
- The post-restart crash has the same `EXC_BREAKPOINT/SIGTRAP` actor-isolation stack on `Window Bounds Observer` through `_dispatch_assert_queue_fail`, `_swift_task_checkIsolatedSwift`, and the display-link callback.
- Antigravity exposes its existing Electron page through a localhost Chrome DevTools endpoint. Its current ephemeral port can be read from `/Users/delimatsuo/Library/Application Support/Antigravity/DevToolsActivePort`; do not hard-code a port or use the authenticated host bridge.
- A read-only Playwright/CDP attachment found exactly one context and one page, titled `Antigravity`, with the existing `Transcriptor` project header, message input, and Gemini model selector.
- A reversible composer proof inserted a local marker, verified Send enabled, cleared the marker, verified Send disabled, and kept the page count at one. It sent no message.
- A bounded round-trip probe was then sent from the Transcriptor project landing page: Antigravity was forbidden to read/edit files, run commands, or use Git and was required to reply with exactly `ANTIGRAVITY_TRANSPORT_READY`.
- The exact readiness token was received. Page count remained one. Antigravity automatically created a temporary `Transport Recovery Probe` conversation from the project landing page; Codex archived that exact recoverable probe and selected the pre-existing `Transcriptor 2` conversation.

## Current State

- Repo/workspace: `/Volumes/Extreme Pro/MYPROJECTS/Transcriptor`
- Task 07 branch: `codex/recruiter-phase`
- Task 07 worktree: `/Volumes/Extreme Pro/MYPROJECTS/Transcriptor`; intentionally dirty and must be preserved
- Task 07 latest commit: `dc262e55f62771615102cef22667f21c3a52a219 docs(builder): add task 07 second repair packet`
- Task 08 worktree: `/Volumes/Extreme Pro/MYPROJECTS/Transcriptor/build/worktrees/task08-iap-source`
- Task 08 branch: `codex/task08-iap-source`
- Task 08 latest commit: `04de2ab4e53e04ed568450d29d43bcbf1d170223 docs(launch): bind current App Hosting source`
- Task 08 dirty state: only untracked protected `frontend/AGENTS.md` and `frontend/CLAUDE.md`; do not read, edit, stage, or remove them
- Draft PR: `#38`, open, clean merge state, exact head `04de2ab4e53e04ed568450d29d43bcbf1d170223`; no merge authorized
- Antigravity: showing its existing main window with the pre-existing `Transcriptor 2` conversation selected and an empty composer. No new window remains; the automatically created transport-probe conversation is archived.

## Newest User Request

The user invoked the handoff workflow and said: “this is not good enough. Find a solution. I closed antigravity. Fix this.” The intent is to repair the Codex Computer Use integration, not merely report the failure.

## Completed Work

- Distinguished the old Antigravity credit/approval problem from the current native crash.
- Verified Antigravity itself remains readable through accessibility and can reopen its existing main window.
- Verified the current ChatGPT/Codex desktop host is `/Applications/ChatGPT.app`, bundle `com.openai.codex`, version `26.825.32147`, build `7303`.
- Verified the signed Computer Use runtime is `/Volumes/Extreme Pro/mac-data/codex/computer-use/Codex Computer Use.app`, bundle `com.openai.sky.CUAService`, version `26.826.1000901`, build `1000901`.
- Verified the Computer Use plugin bundled inside the host and the installed plugin cache are both version `1.0.1000901`; reinstalling only the plugin manifest would not replace the crashing native executable.
- Verified the host is already the newest build in the signed Codex Sparkle feed: `26.825.32147`, build `7303`. There is no newer host update to install.
- Verified the Computer Use service bundle contains the explicit recovery diagnostic: `The Computer Use server and client have a version mismatch. To use Computer Use, ask the user to relaunch their ChatGPT app so that the client will be updated to the latest version.`
- Verified a read-only Antigravity state capture succeeds, but a semantic `set_value` action against the empty message input crashes the native Computer Use pipe before response.
- Verified the latest reproduced crash created `/Users/delimatsuo/Library/Logs/DiagnosticReports/SkyComputerUseService-2026-08-28-225101.ips`, raising the crash-report count from four to five.
- Fully restarted ChatGPT/Codex and verified fresh host/service/client process lifetimes.
- Reproduced the semantic-input crash after restart, creating the sixth report at `2026-08-28T22:54:51-0400`.
- Rejected `SWIFT_IS_CURRENT_EXECUTOR_LEGACY_MODE_OVERRIDE=nocrash` because it would suppress a privileged service's actor-isolation assertion rather than fix the defect.
- Discovered Antigravity's token-free `DevToolsActivePort` locator and attached Playwright to the existing Electron page without creating a page, browser, conversation, or window.
- Completed reversible composer control and an exact-token Antigravity round trip, archived the temporary probe conversation, and restored the pre-existing `Transcriptor 2` conversation.

## In Progress

- Use the qualified existing-window CDP transport for the next exact bounded Antigravity builder packet.
- Codex must continue to inspect all resulting diffs independently, own Git, and preserve the existing source/provider evidence ceiling.

## Task 08 Advance After Transport Recovery

- Fresh read-only App Hosting REST readback bound serving build `build-2026-08-28-006` to archive `rootDirectory=frontend`, state `READY`, and image tag `build-2026-08-28-006`.
- The provider archive path-name audit found zero `AGENTS.md`, zero `CLAUDE.md`, exactly one tracked `.env.example`, and zero other `.env*` paths. The `.env.example` contents were neither extracted nor read.
- Selective protected/environment-excluded extraction produced 84 frontend files. Their path set and byte content matched verification head `7d93c32f55a1b869577dc9fdab8ff030e44ec0a2`; frontend tree `4dbf3cfdaf9ac2d0d3a0c0a815915a61750b7284` is unchanged at final documentation head.
- Sol staff review approved final evidence diff SHA-256 `6b988352552dc7189ac2ffd8bb5dd8eae7597f885c7ba4e7497a01686d34c56b` with no findings.
- Codex committed and pushed `04de2ab4e53e04ed568450d29d43bcbf1d170223`; exact-head CI run `33230809132` passed frontend tests/typecheck/build and backend tests.
- Draft PR #38 remains open, draft, and `CLEAN`. Its evidence ceiling still excludes a 55-minute wall-clock soak, monitoring-email delivery, privacy/legal or production readiness, and merge.

## Important Decisions

- Do not reinstall Antigravity, reset macOS privacy permissions, clear caches, or change Antigravity credits. The crash is in the OpenAI-signed Computer Use service, not Antigravity authentication or TCC.
- Do not install the same current ChatGPT host build as a first step. The signed update feed proves the current host is already latest; the native runtime’s own diagnostic requires a full host relaunch first.
- Do not attempt an unsupported binary patch or Swift-runtime environment override. Native Computer Use still requires a newer fixed OpenAI build or OpenAI support; use Antigravity's existing CDP endpoint as the bounded workaround.
- Use Antigravity through its existing `Transcriptor` project. Do not click New Conversation or open another Antigravity window.
- Discover the ephemeral endpoint from `DevToolsActivePort` after each Antigravity relaunch. Connect to the sole existing page; never call `newPage`, navigate, or use the authenticated host bridge.

## Files And Artifacts

- `/Users/delimatsuo/Library/Logs/DiagnosticReports/SkyComputerUseService-2026-08-28-225101.ips`: newest causal crash report from the semantic-input probe
- `/Volumes/Extreme Pro/mac-data/codex/computer-use/Codex Computer Use.app`: installed signed Computer Use runtime
- `/Applications/ChatGPT.app`: installed ChatGPT/Codex desktop host
- `/Volumes/Extreme Pro/MYPROJECTS/Transcriptor/build/worktrees/task08-iap-source`: qualified Task 08 source worktree
- `/Volumes/Extreme Pro/MYPROJECTS/Transcriptor/docs/handoffs/2026-08-28-task08-antigravity-computer-use-restart-new-session-prompt.md`: paste-ready restart prompt
- `/Users/delimatsuo/Library/Application Support/Antigravity/DevToolsActivePort`: token-free current Electron debugging-port locator; read only the port line needed for localhost CDP attachment

## Commands Run And Results

```bash
git status --short --branch
git rev-parse HEAD
git log -1 --format='%H %s'
```

Result: Task 07 remains on `codex/recruiter-phase` at `dc262e55f62771615102cef22667f21c3a52a219`, intentionally dirty, with no paths staged.

```bash
git -C '/Volumes/Extreme Pro/MYPROJECTS/Transcriptor/build/worktrees/task08-iap-source' status --short --branch
git -C '/Volumes/Extreme Pro/MYPROJECTS/Transcriptor/build/worktrees/task08-iap-source' log -1 --format='%H %s'
gh pr view 38 --json number,title,state,isDraft,headRefName,headRefOid,mergeStateStatus,statusCheckRollup,url
```

Result at restart: Task 08 was at exact head `7d93c32f55a1b869577dc9fdab8ff030e44ec0a2`; it has since advanced to exact head `04de2ab4e53e04ed568450d29d43bcbf1d170223`. Draft PR #38 remains open and `CLEAN`; only the two protected untracked frontend instruction files are present.

```bash
curl -fsSL --max-time 30 'https://persistent.oaistatic.com/codex-app-prod/appcast.xml'
```

Result: newest signed host release is the installed `26.825.32147`, build `7303`.

```text
Computer Use action: get_app_state(com.google.antigravity)
```

Result: succeeded; existing Antigravity main window loaded with project `Transcriptor` selected and empty message input.

```text
Computer Use action: set_value(com.google.antigravity, Message input, "")
```

Result: failed with `Sky Computer Use native pipe closed before response`; a fifth native service crash report appeared at `2026-08-28T22:51:01-0400`.

```text
Post-restart Computer Use action: set_value(com.google.antigravity, Message input, "")
```

Result: failed with the same native-pipe error and created the sixth crash report at `2026-08-28T22:54:51-0400`.

```text
Playwright chromium.connectOverCDP(<port discovered from DevToolsActivePort>)
```

Result: exactly one context/page; Antigravity `Transcriptor` composer controlled and cleared; exact `ANTIGRAVITY_TRANSPORT_READY` round trip succeeded; page count stayed one. The temporary probe conversation was archived and `Transcriptor 2` restored.

## Verification

- Passed: exact host, plugin, and service versions read from signed bundle metadata.
- Passed: current host release compared with the signed production Sparkle appcast.
- Passed: Antigravity read-only accessibility capture and exact existing-project selection.
- Failed: semantic Antigravity UI action; deterministic native pipe crash reproduced.
- Failed after restart: native Computer Use semantic action; crash count increased from five to six.
- Passed: read-only CDP attachment to the existing Antigravity page, with one context and one page.
- Passed: reversible local draft insertion/clear causal proof; no message sent.
- Passed: bounded exact-token Antigravity message round trip; no file/command/Git permission granted and no new page/window created. The temporary auto-created probe conversation was archived, and the pre-existing `Transcriptor 2` conversation was selected.

## Risks And Watchouts

- The CDP port is ephemeral. Rediscover it from `DevToolsActivePort` after an Antigravity relaunch; do not persist a numeric port.
- Do not confuse Antigravity's visible successful read with writable control; only an action plus a no-new-crash check qualifies the repair.
- Do not infer qualification from the Computer Use service auto-respawning. It has already auto-respawned and crashed again.
- Do not treat the CDP workaround as a repair of the OpenAI native service. It bypasses the crashing service while preserving the existing-window constraint.
- Do not disclose or copy `.env*`, credentials, OAuth secrets, raw API keys, or provider secrets while resuming Task 08.

## Do Not Do

- Do not reset, clean, checkout, broadly stage, or otherwise disturb the dirty Task 07 checkout.
- Do not read, edit, stage, or delete `AGENTS.md`, `frontend/AGENTS.md`, or `frontend/CLAUDE.md`.
- Do not merge draft PR #38.
- Do not open a new Antigravity conversation or extra window.
- Do not return to native Computer Use for Antigravity actions until a newer signed service is available and causally verified.
- Do not reset TCC, delete Computer Use state, uninstall the authorization plugin, or patch the signed native binary.
- Do not set Swift executor compatibility overrides, use the authenticated Antigravity host bridge, create a page, navigate the existing page, or open a new conversation.

## Next Recommended Steps

1. Read the current numeric port from the first line of Antigravity's `DevToolsActivePort` file without printing or retaining other private process values.
2. Attach Playwright with `chromium.connectOverCDP` to localhost and require exactly one existing Antigravity context/page.
3. Require both the existing `Transcriptor` header and empty message composer before writing a bounded builder packet.
4. Send only the exact authorized builder packet; do not create/navigate pages or click New Conversation.
5. Independently inspect the exact authorized file diff and run Codex-owned verification. Antigravity remains a bounded builder and must not own Git.
6. Re-discover the port after every Antigravity relaunch. If the sole-page/context or Transcriptor assertions fail, stop rather than guessing.

## Open Questions

- When will OpenAI ship a signed Computer Use service that fixes the `Window Bounds Observer` Swift executor crash? Until then, use the qualified existing-window CDP path.
