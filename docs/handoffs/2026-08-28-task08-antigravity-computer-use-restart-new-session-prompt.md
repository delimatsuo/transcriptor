You are continuing the Transcriptor Task 08 Antigravity Computer Use integration repair.

Current objective:
Use the qualified existing-window Antigravity CDP transport for bounded builder work. Native OpenAI Computer Use remains blocked by a reproducible signed-service actor-isolation crash even after a full ChatGPT/Codex restart.

Workspace:
- Repo/worktree: `/Volumes/Extreme Pro/MYPROJECTS/Transcriptor`
- Task 07 branch: `codex/recruiter-phase`
- Task 08 worktree: `/Volumes/Extreme Pro/MYPROJECTS/Transcriptor/build/worktrees/task08-iap-source`
- Task 08 branch: `codex/task08-iap-source`
- Important docs to read first:
  - `/Volumes/Extreme Pro/MYPROJECTS/Transcriptor/docs/handoffs/2026-08-28-task08-antigravity-computer-use-restart-handoff.md`

Newest user request:
The user said: “this is not good enough. Find a solution. I closed antigravity. Fix this.” Treat this as authorization to repair and verify the integration, not merely diagnose it.

Current state:
- Task 07 is intentionally dirty at `dc262e55f62771615102cef22667f21c3a52a219`; preserve it.
- Task 08 is at exact head `04de2ab4e53e04ed568450d29d43bcbf1d170223`; draft PR #38 is open and clean. Exact-head CI run `33230809132` passed. Do not merge.
- Current App Hosting build `build-006` is bound to the exact 84-file protected/environment-excluded frontend projection. This is not a full-archive or `.env.example`-content claim.
- ChatGPT/Codex host before restart: `26.825.32147`, build `7303`; this is the newest release in the signed production appcast.
- Computer Use runtime before restart: `26.826.1000901`, build `1000901`.
- The runtime contains an explicit client/server mismatch diagnostic directing a full ChatGPT relaunch.
- Read-only Antigravity capture works; semantic input crashes the signed service with `Sky Computer Use native pipe closed before response`.
- Latest pre-restart crash: `/Users/delimatsuo/Library/Logs/DiagnosticReports/SkyComputerUseService-2026-08-28-225101.ips`; five reports existed.
- Full ChatGPT/Codex restart did not fix native Computer Use. A post-restart semantic action created `/Users/delimatsuo/Library/Logs/DiagnosticReports/SkyComputerUseService-2026-08-28-225451.ips` with the same actor-isolation crash.
- Antigravity's existing Electron page is controllable through the ephemeral localhost port identified by `/Users/delimatsuo/Library/Application Support/Antigravity/DevToolsActivePort`.
- Playwright/CDP found exactly one context and one existing page. Reversible composer control passed, and an exact-token `ANTIGRAVITY_TRANSPORT_READY` round trip succeeded.
- Sending from the Transcriptor project landing page automatically created a temporary `Transport Recovery Probe` conversation. It was archived, and the pre-existing `Transcriptor 2` conversation was restored with an empty composer. No new Antigravity window remains.

Critical constraints:
- Do not revert, reset, clean, checkout, or broadly stage user/other-agent changes.
- Do not read, edit, stage, or delete `AGENTS.md`, `frontend/AGENTS.md`, or `frontend/CLAUDE.md`.
- Do not inspect or copy `.env*`, credentials, OAuth secrets, API-key values, or provider secrets.
- Do not open a new Antigravity window or conversation; use the left-side existing `Transcriptor` project.
- Do not use native Computer Use for Antigravity actions until a newer signed service is available and causally verified.
- Do not hard-code the CDP port, call `newPage`, navigate, use the authenticated host bridge, or open a new Antigravity conversation.
- Do not reset TCC, delete Computer Use state, uninstall plugins, or patch signed binaries.
- Do not merge PR #38.

Facts and evidence:
- The host is already latest; reinstalling the same host is not the first repair.
- The Computer Use service auto-respawned previously and still crashed, so respawn alone is not proof.
- The native crash is `EXC_BREAKPOINT/SIGTRAP` on `Window Bounds Observer` through `_dispatch_assert_queue_fail`, `_swift_task_checkIsolatedSwift`, and a display-link callback.
- A new crash report appeared immediately after the semantic Antigravity `set_value` probe.

Next recommended action:
1. Read the first line of `DevToolsActivePort` to discover the current localhost CDP port without printing private process values.
2. Attach Playwright with `chromium.connectOverCDP` and require exactly one existing Antigravity context/page.
3. Require the active `Transcriptor` header and an empty message composer.
4. Send only the exact authorized builder packet in the existing conversation.
5. Codex independently reviews the exact allowlisted diff, runs verification, and owns Git.

Verification expected:
- Exactly one existing Antigravity context/page before and after the action
- Existing `Transcriptor` project/header bound before sending
- Exact authorized builder response and exact allowlisted diff
- Codex-owned tests, diff review, and Git state verification

Known risks:
- The numeric CDP port changes across Antigravity launches; rediscover it every time.
- The workaround does not repair native Computer Use; a newer signed OpenAI service is still required.
- Do not let Antigravity open an extra window or conversation.

If anything conflicts, the newest user request wins. Start by running:

```bash
cd "/Volumes/Extreme Pro/MYPROJECTS/Transcriptor"
git status --short --branch
```
