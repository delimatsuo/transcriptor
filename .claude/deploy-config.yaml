# .claude/deploy-config.yaml
# Generated by /deploy-setup — consumed by /deploy
# Do not delete — /deploy relies on this file for project-specific values.
#
# PHASE 0B CONTAINED / DEPLOYMENT UNCONFIGURED: The remote workflow, GitHub WIF
# provider, and deploy service account were disabled on 2026-07-15. No project
# in this file is an approved deployment target. See README.md and
# docs/current-state/phase-0b-containment-evidence.md.

schema_version: 1

project:
  name: "transcriptor"
  platform: "firebase-hosting+cloud-run"
  # Target web workspace: Firebase Hosting (not configured; firebase.json absent)
  # Target cloud control plane: Cloud Run (current container still couples local capture)
  # Target device capture: native macOS companion, outside this deploy scaffold

environments:
  # Safety sentinel: neither release environment has an approved project.
  staging:
    branch: "staging"
    url: "pending"
    gcp_project_id: "UNCONFIGURED_DO_NOT_DEPLOY"
    health_check: "/api/sessions"
  production:
    branch: "main"
    url: "pending"
    gcp_project_id: "UNCONFIGURED_DO_NOT_DEPLOY"
    health_check: "/api/sessions"

ci_cd:
  provider: "github-actions"
  deploy_workflow: ".github/workflows/deploy.yml"
  rollback_workflow: ".github/workflows/rollback.yml"
  auth_method: "disabled"

reviewer:
  tech_stack: "Next.js, TypeScript, React, Firebase, Python 3.12, FastAPI, Google Cloud Speech-to-Text v2, Gemini, Firestore, Cloud Run, WebSocket"
  additional_rules: "BLOCK deploy. The legacy project transcriptor-490222 is inventory-only; an isolated development project and a new review are still required."
